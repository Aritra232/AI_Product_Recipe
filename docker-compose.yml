services:
  ai-search:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tomatoes-ai-search
    env_file:
      - .env
    ports:
      - "8005:8005"
    volumes:
      - ./Service/CSV:/app/Service/CSV
      - ./Service/data:/app/Service/data
    restart: unless-stopped

